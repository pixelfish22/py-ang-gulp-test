language: node_js
node_js:
- "0.10"
python:
- '2.7'
rvm:
- 2.0.0
before_install:
  - npm install -g npm
  - npm install -g bower



install:
- gem install sass

before_script:
  - npm install -g npm
  - npm install -g bower
  - echo -e "Host github.com\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
  - bower install -f


script:
- gulp lint

